## Design Context

### Users
1인 운영자(개발자 본인)가 SRT 취소표/예약대기 매크로 실행 중 지켜보는 도구. 티켓 오픈창처럼 시간이 촉박한 순간에 사용하며, 좌석 발견 시 빠르게 인지하고 반응해야 한다.

### Brand Personality
차분하고 빠르고 실용적. 마케팅용 "브랜드"가 아니라 운영 도구 — 화려함보다 즉각적 인지·신뢰가 우선.

### Aesthetic Direction
다크 테마 오퍼레이터 대시보드 유지 (SRT 버건디 `#761232` 브랜드 컬러 앵커). 시스템 폰트 스택 유지 — 네트워크 폰트 로딩 없이 즉시 렌더링되는 현재 방식이 실전 예매 순간에 유리하므로 타입 스케일·굵기·간격 체계만 다듬어 위계를 명확히 한다. 로그 스트림은 mono 폰트 유지.

### Design Principles
- 폰트 교체 없음 — 시스템 폰트 스택(-apple-system 등) 유지, 타입 스케일·굵기·행간만 체계화한다.
- 5단계 이하의 명확한 rem 기반 타입 스케일, 단계 간 최소 1.25배 대비를 확보한다.
- 핵심 순간(좌석 발견 로그, 상태 배지)은 크기만이 아니라 굵기·색·자간을 함께 조합해 위계를 표현한다.
- 앱 UI이므로 fluid clamp() 대신 고정 rem 스케일을 사용한다 (필요 시 1~2개 브레이크포인트에서만 조정).
- 로그(mono)와 UI(시스템 산세리프) 두 폰트 역할만 유지하고 새 폰트 패밀리를 도입하지 않는다.
